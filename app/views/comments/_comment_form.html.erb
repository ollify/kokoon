<%= simple_form_for([flat, rental, ticket, comment], remote: true) do |f| %>
  <%= f.input :content, as: :string, placeholder: "Your comment goes here", label: "Your comment" %>
  <div class="button-box"><%= f.submit class: 'btn-secondary' %></div>
<% end %>
